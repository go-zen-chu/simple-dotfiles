version: 2 # use CircleCI 2.0
jobs:
  build:
    docker:
      - image: centos:7
    steps:
      - checkout
      - run:
          name: "setup"
          command: |
            ./setup.sh
      - run:
          name: "uninstall"
          command: |
            ./setup.sh -u